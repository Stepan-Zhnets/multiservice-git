[project]
name = "multiservice-git"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flet[all]>=0.27.6",
]

[dependency-groups]
dev = [
    "ruff>=0.11.5",
]
